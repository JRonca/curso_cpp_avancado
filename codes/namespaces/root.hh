#ifndef ROOT_H
#define ROOT_H
namespace root{
	class Terminal{
		public:
			void terminal(){
				std::cout << "Minha funcao membro numero 1" << "\n";
			}

			void root(){
				std::cout << "Minha funcao membro numero 2" << "\n";
			}
	};
}
#endif
